<?xml version="1.0" encoding="utf-8"?>
<!--
    Конфигурация сетевой безопасности Android.

    В DEBUG-сборках разрешён cleartext (http/ws) для локального сервера.
    В RELEASE-сборках разрешён только HTTPS/WSS.

    Для продакшена:
      1. Замените android:usesCleartextTraffic="false" в AndroidManifest.xml
      2. Раскомментируйте секцию <pin-set> и укажите ваши сертификаты.
-->
<network-security-config>

    <!-- Конфигурация для продакшена: только TLS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!-- DEBUG: разрешаем localhost и локальные IP -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>  <!-- для отладки с прокси -->
        </trust-anchors>
    </debug-overrides>

    <!--
    Certificate Pinning — раскомментируйте для продакшена.
    Получить pin: openssl s_client -connect yourdomain.com:443 |
                  openssl x509 -pubkey -noout | openssl pkey -pubin -outform der |
                  openssl dgst -sha256 -binary | base64

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">yourdomain.com</domain>
        <pin-set expiration="2026-01-01">
            <pin digest="SHA-256">YOUR_PRIMARY_PIN_BASE64=</pin>
            <pin digest="SHA-256">YOUR_BACKUP_PIN_BASE64=</pin>
        </pin-set>
    </domain-config>
    -->

    <!-- Разрешаем cleartext только для локальной сети в DEBUG -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">192.168.0.0</domain>
        <domain includeSubdomains="false">192.168.1.0</domain>
    </domain-config>

</network-security-config>
